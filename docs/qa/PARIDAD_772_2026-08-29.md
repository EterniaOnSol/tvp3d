# Auditoria de paridad TVP 7.72

Fecha: 2026-08-29

## Criterio

Esta auditoria no considera una mecanica terminada porque se parezca a Tibia.
Solo usa `COMPROBADO` cuando el payload o la regla se obtuvo del servidor TVP,
el cliente conserva el estado y existe una prueba que detecta desalineacion o
regresion. `PARCIAL` significa que existe codigo real pero falta alguna de esas
piezas. `AUSENTE` significa que no hay recorrido utilizable.

Fuentes autoritativas revisadas:

- `servidor/src/protocolgame.cpp` para opcodes y payloads 7.72.
- `servidor/src/game.cpp`, `creature.cpp`, `player.cpp` y `monster.cpp` para
  reglas de juego, muerte, corpse y loot.
- `servidor/data/` para acciones, fluidos, NPC y datos de monstruos.
- `cliente3d/red/` y `cliente3d/ui/` para consumo, estado e interaccion.

## Estado del checkpoint

| Bloque | Estado | Evidencia y limite actual |
|---|---|---|
| Login, mapa y movimiento | COMPROBADO | Login real, mapa `0x64`, franjas, pasos, autowalk y cambios de piso probados. |
| Render 3D y pisos | COMPROBADO con deuda visual | Pruebas de formas, arquitectura visible desde niveles superiores, cama y farol pasan. Perfiles authored adicionales siguen siendo trabajo visual. |
| Inventario y equipo | COMPROBADO | `0x78/0x79`, cantidades, liquid subtype y movimientos exactos. |
| Contenedores y corpses | COMPROBADO | `0x6E` a `0x72`, capacidades hasta `u8`, drag y split de pilas pasan. La prueba viva mata un monstruo, abre su corpse como contenedor y lee su loot; repetible con `--solo-loot`. |
| Pilas de items | COMPROBADO | El count viaja y se conserva; el selector envia una cantidad exacta sin que el cliente decida el resultado. |
| Use/use-with/look | COMPROBADO para recorridos actuales | Puertas simples, runas, mana fluid y look existentes usan los mensajes reales. Variantes de puertas parametrizadas requieren casos vivos. |
| Trade entre jugadores | COMPROBADO | Dos clientes reales presentaron ambas ofertas, aceptaron y recibieron la transferencia autoritativa de objetos. `protocolo-red` 1.5.0 publico el `0x7D` saliente y la prueba viva ya lo usa en vez de armar bytes; `cliente` 1.4.0 lo ofrece como `Trade with <nombre>` en el menu de criatura. Falta prueba viva del camino que empieza en ese menu. |
| Canales | COMPROBADO en parser/UI | Lista, abrir/cerrar, privado y hablar usan `0x97` a `0x9A`, `0xAA`, `0xAB` a `0xAD` y `0xB2/0xB3`; prueba concatenada demuestra alineacion. |
| VIP | COMPROBADO en vivo | Dos clientes reales verificaron remove/add por nombre y GUID, offline `0xD2`, online `0xD3` al entrar y offline `0xD4` al salir. |
| Combate basico | PARCIAL | Ataque/seguir, HP, efectos, proyectiles, stats, skills e iconos existen. Cancel target, modos confirmados y estados visuales completos de criatura no estan cerrados. |
| Velocidad/skull/party shield | COMPROBADO | `AddCreature` y `0x8D/0x8F/0x90/0x91` conservan luz, velocidad, skull y shield; `estado_criatura_self_test.gd` cubre payload completo, truncados, desconocidos y giro corto. La UI ya los muestra: calavera y escudo en el Battle List y en el Target con las tablas de `const.h`, y `Speed` de Skills con la velocidad confirmada de `mi_id`. Falta llevarlos sobre la criatura en el mundo 3D, que aun no tiene placa de nombre. |
| Party | COMPROBADO | `protocolo-red` 1.4.0 transporta las seis ordenes `0xA3` a `0xA8` con los payloads de `protocolgame.cpp:1171-1207`, y `cliente` 1.3.0 las ofrece en el menu del Battle List segun los escudos confirmados. Este servidor no manda ningun paquete de party: lo unico observable es el escudo `0x91`. La prueba viva con dos clientes recorrio invitar, unirse, pasar liderazgo y salir, y los escudos de los dos lados contaron la misma party en cada paso. La experiencia compartida `0xA8` tiene transporte pero no boton: el cliente 7.72 no lo tenia. |
| Condiciones | PARCIAL | `0xA2` conserva iconos y stats se actualizan. No todas las consecuencias visibles y temporales tienen prueba viva. |
| Muerte y respawn | COMPROBADO | El cliente consume `jugador_muerto`, bloquea intenciones, muestra la reentrada y envia logout `0x14`; `prueba_muerte_reentrada` cubre el consumo con bytes exactos y la prueba viva reprodujo muerte por demon, corpse `dead human`, cierre de sesion y reingreso vivo en el templo. La precondicion de pelear fuera de zona de proteccion ya la resuelve el servidor con `/c`, asi que la corrida completa se repite sola. Al repetirla aparecio una carrera que ya quedo cerrada: `protocolo-red` 1.2.0 emite la muerte por casilla y vida autoritativa, sin depender del indice de pila, y la causa de fondo era el `0x64` desalineado que arreglo 1.3.0. |
| Loot | COMPROBADO | `Creature::dropCorpse` crea el contenedor y `Monster::dropLoot` lo llena. La prueba viva mato un rat, abrio su corpse con `0x82` y leyo el loot del servidor en dos corridas independientes. |
| Tiendas NPC | COMPROBADO como dialogo 7.72; no hay shop window nativa | Esta rama declara el modo TALK previo a 8.2. El C++ no expone paquetes de shop; no se debe inventar una ventana que el servidor no envia. |
| Depot | COMPROBADO | Prueba viva en el depot de **Thais**, que es donde salen los personajes de prueba; Rookgaard no tiene depot ni correo, igual que en el Tibia original. Se guarda un objeto, se cierra la sesion, se vuelve a entrar y el objeto sigue en el `depot chest`. Tres reglas de esta rama quedaron escritas en `docs/qa/PRUEBA_VIVA_DEPOT.md`: hay que **pisar** la baldosa para que el servidor cargue el depot del jugador, las cosas viven en el `depot chest` de adentro del locker, y la ventana de contenedor la elige el cliente en el `0x82`. Sin pisar la baldosa se abre el mueble del mapa, que acepta objetos y no es de nadie. |
| Mail/parcels | COMPROBADO | La guarda de `game.cpp:2556-2560` ya deja pasar items legibles. La ventana abre, guarda y relee la etiqueta; la etiqueta entra en la parcel, el mailbox la retira y la entrega aparece persistida en el depot 1 del destinatario. El usuario confirmo el recorrido vivo de punta a punta y los archivos persistidos conservan parcels dirigidas en los depots de prueba. Detalle en `docs/qa/PRUEBA_VIVA_PARCEL.md`. |
| Casas/camas | BLOQUEADO | Casa 6 y camas reales localizadas; la prueba viva recibe `You cannot use this object` al usar la cama aun con propietario y premium temporales. Se restauraron owner/premium. Falta resolver zona PZ/permisos antes de certificar dormir/despertar y persistencia. |
| Quest log/modal/text windows | AUSENTE o PARCIAL | Quests por scripts funcionan como autoridad; respuestas de ventanas y modal `0xFA` no estan cerradas en UI. |
| Monstruos que mantienen ataque | COMPROBADO | El servidor reconstruido conserva el objetivo durante una desaparicion temporal y lo reacquiere al volver. `prueba_reacquisicion_monstruo.tscn` recibio un golpe nombrado de `cave rat`, movio al jugador 39 SQM fuera de vista y exigio otro golpe nombrado del mismo id al regresar. |

## Flujo exacto de muerte encontrado

Esta rama no contiene `sendDeath` ni `sendReLoginWindow`. El recorrido real es:

1. `Creature::onDeath` crea splash/corpse, ejecuta eventos y deposita loot.
2. `Player::death` fija el templo como proximo login, aplica perdidas y
   blessings, restaura HP/MP y elimina condiciones.
3. Si se creo corpse, `Game::removeCreature(player, false)` retira al jugador
   de la casilla y envia a los espectadores el `0x6C` normal de remove thing.
4. La conexion del jugador no se cierra en esa ruta. Si despues recibe logout
   `0x14`, `ProtocolGame::logout` detecta que ya fue removido y desconecta.

El contrato de implementacion para el cliente es por tanto detectar que el
objeto eliminado por `0x6C` es la criatura `mi_id`, emitir un evento local de
muerte, bloquear nuevas intenciones, mostrar la accion de volver y enviar
logout `0x14` para regresar al selector. No debe esperarse ni inventarse un
opcode de muerte que este servidor no produce.

El corpse del monstruo conserva propietario de mayor dano cuando corresponde,
se abre mediante el flujo normal de contenedor y decae bajo autoridad del
servidor. La prueba viva debe verificar tambien que otro cliente no obtiene
acceso anticipado si la regla de ownership lo impide.

## Matriz ejecutada antes del checkpoint

Ejecutable: Godot 4.7.2 estable, modo headless.

| Caso | Resultado |
|---|---|
| `prueba_contenedores.tscn` | 0 fallas |
| `test_controles.tscn` | 0 fallas |
| `prueba_eventos_visuales.tscn` | 0 fallas |
| `prueba_modelos_cama.gd` | OK: cama, cama ocupada y farol |
| `test_formas_render.tscn` | 0 fallas |
| `prueba_spells_animaciones.tscn` | 0 fallas |
| `main.tscn --quit-after 2` | codigo 0 |
| Escaneo editor Godot | codigo 0 |

`git diff --check` no encontro errores; Git solo aviso la normalizacion futura
LF/CRLF. Esta ejecucion no compilo el servidor C++ ni sustituyo una prueba viva.

## Ejecutado despues del checkpoint

El 2026-08-29 se cerraron los dos primeros puntos del orden verificable.
`protocolo-red` conserva velocidad, skull, shield y luz de criatura; el
carril `cliente` consume `EstadoMundo.jugador_muerto` con pantalla de
reentrada y logout `0x14`; y QA corrio la prueba viva de muerte, corpse y
loot contra el servidor. Evidencia y limites en
`docs/qa/PRUEBA_VIVA_MUERTE_LOOT.md`. La matriz local termina 10/10.

## Ejecutado el 2026-08-29 por la tarde

Los puntos 1 y 2 del orden verificable quedaron cerrados:

1. La UI muestra velocidad, calavera y escudo de party. `cliente` publico su
   contrato 1.2.0 y `prueba_estado_criatura_ui` entra a la matriz local, que
   termina 11/11.
2. La precondicion de la prueba viva de muerte es automatica: el god va al
   campo y trae al personaje con `/c`, una talkaction del servidor, sin que el
   cliente camine ni simule intenciones. `--solo-campo` la repite sin costarle
   un nivel a nadie y devuelve el personaje a su templo.

Repetir el duelo destapo dos fallos que la precondicion manual tapaba: la
carrera de la senal de muerte descrita arriba y un corpse de monstruo que llego
sin nombre resoluble. Ambos estan en `docs/qa/PRUEBA_VIVA_MUERTE_LOOT.md` con
su evidencia.

## Cerrado el 2026-08-29 al mediodia

- La carrera de la senal de muerte: `protocolo-red` 1.2.0 la emite por casilla
  y vida autoritativa, sin depender del indice de pila.
- El `0x64` desalineado, que era la causa de fondo de esa carrera y de las
  pilas corridas: `protocolo-red` 1.3.0 cuenta la casilla que el servidor
  describe con cero bytes.
- Trade y VIP con dos clientes reales.
- Party completa: transporte, interfaz y prueba viva de dos clientes.

## Siguiente orden verificable

1. Publicar en `protocolo-red` el `0x7D` saliente de trade, que hoy la prueba
   viva arma a mano.
2. Cerrar parcels, mailbox, depot y casas con persistencia real.
3. Cerrar text/house/modal windows y luego quests que dependan de ellas.
4. Llevar calavera y escudo tambien sobre la criatura en el mundo 3D, que
   todavia no tiene placa de nombre.

Ningun bloque posterior debe marcarse 1:1 sin prueba de protocolo y, cuando
corresponda, prueba viva contra el servidor autoritativo.

## Correccion compensatoria posterior

La carrera de muerte del orden anterior queda cerrada por `protocolo-red`
1.2.0. Dos corridas vivas posteriores confirmaron vida cero, evento de muerte,
logout `0x14`, cierre del servidor y reingreso vivo. Por tanto, "Muerte y
respawn" pasa a **COMPROBADO**.

El siguiente bloque verificable cambia a la alineacion del mapa inicial. Los
self-tests sinteticos pasan, pero el `0x64` real del campo deja al jugador
fuera de `mi_pos` y consume bytes posteriores como client ids imposibles. El
orden vigente es:

1. Corregir y probar en vivo el cierre/salto de `GetMapDescription` hasta que
   `mapa_alineado` permanezca verdadero y no existan ids fuera del catalogo.
2. Publicar en `protocolo-red` la solicitud saliente `0x7D` y conectarla a una
   interaccion de produccion; la transferencia y VIP ya pasaron en vivo.
3. Implementar party completa segun `Game`/`Party` y sus opcodes 7.72.
4. Cerrar parcels, mailbox, depot y casas con persistencia real.
5. Cerrar text/house/modal windows y luego las quests dependientes.

## Correccion compensatoria: trade y VIP vivos

El segundo punto se ejecuto con `prueba_trade_vip_vivo.tscn`. La corrida final
abrio dos sesiones, obtuvo la secuencia VIP offline -> online -> offline y
completo un intercambio aceptado en ambos lados. No se considero suficiente el
cierre `0x7F`: la prueba exigio tambien los cambios de inventario que demuestran
la transferencia de los dos server id 2006/client id 2874. Termino en codigo 0.

La auditoria destapo un hueco de produccion: `Conexion772` no contiene el
metodo que envia la solicitud inicial de trade. QA uso el payload exacto de
`ProtocolGame::parseRequestTrade` sin tocar el carril ocupado. Por eso VIP pasa
a COMPROBADO y trade queda PARCIAL hasta que `protocolo-red` publique ese
iniciador y el cliente lo ofrezca desde el mundo.

## Cierre compensatorio: parcels y reacquisicion

- El usuario confirmo la entrega viva de parcels y los archivos persistidos de
  los personajes muestran las parcels dirigidas dentro de depot 1. Mail y
  parcels pasan a `COMPROBADO`; no se repitio una prueba mutante ya ejecutada.
- El cambio de `monster.cpp` ya estaba compilado en la imagen activa. La nueva
  prueba viva descarto primero una falsa evidencia causada por un spider
  silvestre y endurecio su oracle: solo cuenta dano cuyo mensaje nombra al
  `cave rat` invocado. La corrida final termino en codigo cero con vida
  `133 -> 131`, salida de vista, regreso y vida `131 -> 128`, conservando el
  mismo id `1073764894` y verificando su limpieza.
- El siguiente bloque verificable queda en casas/camas y sus permisos y
  persistencia; despues siguen modal/house windows y quests dependientes.
